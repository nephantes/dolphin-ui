/*
 *Author: Nicholas Merowsky
 *Date: 07 May 2015
 *Ascription:
 */

function postFastlane(id_array, info_array) {
   if (info_array.length == 9) {
      // Barcode Seperation
	  
	  
      alert(info_array);
   }else{
      // No Barcode Seperation
      alert(info_array);
   }
}

function checkFastlaneInput(){
	
}